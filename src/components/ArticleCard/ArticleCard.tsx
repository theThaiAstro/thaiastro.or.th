import React from 'react';

type Props = {
	title: string;
	excerpt?: string;
	coverImage?: string;
};

const ArticleCard: React.FC<Props> = (props: Props) => {
	return <div></div>;
};

export default ArticleCard;
